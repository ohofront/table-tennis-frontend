# Backend origin (leave empty to use same-origin proxy rewrites, or set to direct API URL)
NEXT_PUBLIC_API_BASE_URL=https://table-tennis-api-production.up.railway.app
# Name of the backend-issued httpOnly refresh cookie (presence is only a UX hint).
AUTH_REFRESH_COOKIE_NAME=refreshToken
